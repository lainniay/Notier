# TODO

- [ ] Option 2: Add a sixth generic native `Notier.app` (for example, `com.notier.sender.default`), route unknown app names and known apps with missing native bundles through it, request notification authorization once through that app, then remove the `terminal-notifier` dependency.
